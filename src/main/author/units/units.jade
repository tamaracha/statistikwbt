div(uib-collapse="units.collapse"): form.form-horizontal(novalidate name="units.form")
  .form-group
    label.col-xs-2.control-label(for="selectUnit") Kapitel
    .col-xs-10
      select.form-control#selectUnit(
        name="selectUnit"
        ng-model="units.selected"
        ng-options="u.title for u in units.units track by u._id"
        ng-change="units.select()"
      )
        option(value="") Neues Kapitel
  .form-group(ng-if="units.selected")
    .col-xs-offset-2.col-xs-10: .btn-group
      a.btn.btn-link(ui-sref="main.content.unit.description({unit: units.selected._id})") Anschauen
      button.btn.btn-danger(type="button" ng-click="units.remove()")
        span.fa.fa-times
        span Löschen
h3
  span(ng-bind="units.selected ? units.selected.title : 'Neues Kapitel anlegen'")
  button.btn.btn-default(type="button" ng-click="units.collapse=!units.collapse")
    span.fa(ng-class="units.collapse ? 'fa-toggle-off' : 'fa-toggle-on'" aria-hidden="true")
    | Übersicht {{units.collapse ? 'einblenden' : 'ausblenden'}}
div(ui-view)
