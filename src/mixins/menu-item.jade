mixin menu-item(state, text,icon)
  li(data-ui-sref-active="active"): a(data-ui-sref=state)
    if icon
      span.glyphicon(class="glyphicon-"+icon aria-hidden="true")
      |  #{text}
    else
      | #{text}