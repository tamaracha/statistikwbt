mixin checkbox-inline(name,label,options)
	.form-group
		.col-xs-2.control-label=label
		.col-xs-10: each option, ext in options
			input(type="checkbox" name=name id="#{name}-#{ext}")&attributes(attributes)
			label(for="#{name}-#{ext}")=option